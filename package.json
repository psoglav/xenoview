{
  "name": "cryptoview",
  "author": "psoglav",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "scripts": {
    "build": "yarn remove-build && tsc && yarn copy-public",
    "dev": "cd sandbox && yarn dev",
    "watch": "tsc --watch",
    "test": "jest",
    "prepare": "husky install",
    "remove-build": "rm -rf ./dist",
    "copy-public": "cp -r ./src/public/ ./dist/"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "husky": "^8.0.1",
    "jest": "^28.1.3"
  },
  "prettier": {
    "trailingComma": "all",
    "tabWidth": 2,
    "semi": false,
    "singleQuote": true
  }
}
